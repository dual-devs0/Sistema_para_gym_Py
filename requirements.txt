# Sistema para GymPro — dependencias del proyecto
#
# Para correr la app completa: docker compose up --build
#
# Dependencias por componente:

# ── Backend (Python) ──
# Instalación: cd backend && pip install -r requirements.txt
# Python 3.11+, PostgreSQL 15+, Redis 7+
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
pyjwt==2.9.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
redis[hiredis]==5.1.1
httpx==0.27.2
structlog==24.4.0
sentry-sdk==2.14.0
email-validator==2.2.0
# Dev
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.6.4

# ── Frontend (Node.js) ──
# Instalación: cd frontend && npm install
# Node.js 20+
# Dependencias en frontend/package.json:
#   react 19, react-router-dom 7+
#   @tanstack/react-query ^5.56
#   motion (framer-motion v12+)
#   axios
#   zustand
#   tailwindcss v4, @tailwindcss/vite
#   vite 7, typescript 5+
